name: CodeQL

on:
  push:
    branches: [main, 'feat/**']
  pull_request:

permissions:
  contents: read

jobs:
  analyze:
    name: CodeQL
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: thepja/template-ci-cd/.github/workflows/codeql.yml@1b0e2768804a51aad1f486190f180cb8344669a7 # template-ci-cd
    with:
      languages: java-kotlin
